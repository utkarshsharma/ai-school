# AI School Backend Environment Configuration

# ===================
# API Keys (Required)
# ===================

# Gemini API Key - Get from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud TTS API Key - Get from https://console.cloud.google.com/apis/credentials
# (Enable "Cloud Text-to-Speech API" first, then create an API key)
GOOGLE_TTS_API_KEY=your_tts_api_key_here

# ===================
# Service URLs (defaults work for local dev)
# ===================

REMOTION_SERVICE_URL=http://localhost:3000

# ===================
# Storage (defaults work for local dev)
# ===================

STORAGE_BASE_PATH=./storage

# SQLite (default for local dev):
DATABASE_URL=sqlite:///./storage/ai_school.db

# PostgreSQL (for production):
# DATABASE_URL=postgresql://ai_school:ai_school@localhost:5432/ai_school

# ===================
# Queue (defaults work for local dev)
# ===================

# Redis URL (optional, uses in-memory queue if not set)
# REDIS_URL=redis://localhost:6379/0

# Number of worker threads for parallel job processing (default: 4)
WORKER_COUNT=4

# ===================
# Parallel Processing (defaults work for local dev)
# ===================

# Maximum concurrent Gemini API calls (default: 3)
GEMINI_MAX_CONCURRENT=3

# Number of parallel image generation workers (default: 4)
IMAGE_WORKERS=4

# Number of parallel TTS generation workers (default: 4)
TTS_WORKERS=4

# ===================
# Retry Configuration (defaults provide resilience)
# ===================

# Gemini API retry settings
GEMINI_MAX_RETRIES=3
GEMINI_RETRY_BASE_DELAY=2.0

# Image generation retry settings
IMAGE_MAX_RETRIES=3
IMAGE_RETRY_BASE_DELAY=2.0

# TTS retry settings
TTS_MAX_RETRIES=3
TTS_RETRY_BASE_DELAY=1.0

# ===================
# Server (defaults work for local dev)
# ===================

BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
DEBUG=false
